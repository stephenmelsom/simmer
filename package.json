{
  "name": "simmer",
  "private": true,
  "version": "0.1.0",
  "description": "Aviation/radio homelab dashboard, gated by Pocket ID group",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=web & wait",
    "build": "npm run build --workspace=web && npm run build --workspace=server",
    "start": "npm run start --workspace=server",
    "check": "npm run check --workspace=server && npm run check --workspace=web"
  }
}
